let preço = 1000;

if (preço > 2000){
    let desconto = preço*10/100;
    console.log("Seu sálario com o desconto de 10% é " + (preço-desconto));
}else{
    let Valor1 = preço*5/100;
    console.log("Seu salario com o desconto de 5% é " + (preço-Valor1));

}